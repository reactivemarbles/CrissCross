<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CrissCross.WPF.UI.Controls"
    xmlns:core="clr-namespace:CrissCross;assembly=CrissCross">

    <Style TargetType="{x:Type controls:SegmentedControl}">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate DataType="{x:Type core:SegmentItem}">
                    <RadioButton
                        MinHeight="32"
                        Margin="0"
                        Padding="12,4"
                        Command="{Binding SelectionChangedCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:SegmentedControl}}}"
                        CommandParameter="{Binding Key}"
                        Content="{Binding Text}"
                        IsEnabled="{Binding IsEnabled}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SegmentedControl}">
                    <Border
                        Padding="2"
                        BorderBrush="#22000000"
                        BorderThickness="1"
                        CornerRadius="8">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
