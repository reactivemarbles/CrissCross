<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CrissCross.WPF.UI.Controls"
    xmlns:core="clr-namespace:CrissCross;assembly=CrissCross">

    <Style TargetType="{x:Type controls:ReactiveFormField}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ReactiveFormField}">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,4">
                            <TextBlock
                                DockPanel.Dock="Right"
                                Margin="6,0,0,0"
                                Opacity="0.72"
                                Text="{Binding State, RelativeSource={RelativeSource TemplatedParent}}" />
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter Content="{TemplateBinding Header}" />
                                <TextBlock
                                    x:Name="RequiredIndicator"
                                    Margin="4,0,0,0"
                                    Foreground="#C42B1C"
                                    Text="*"
                                    Visibility="Collapsed" />
                            </StackPanel>
                        </DockPanel>
                        <Border
                            Padding="1"
                            BorderBrush="#33000000"
                            BorderThickness="1"
                            CornerRadius="4">
                            <ContentPresenter Content="{TemplateBinding Content}" />
                        </Border>
                        <TextBlock
                            Margin="2,4,0,0"
                            Opacity="0.72"
                            Text="{TemplateBinding HelperText}" />
                        <ItemsControl
                            Margin="2,4,0,0"
                            ItemsSource="{Binding Messages, RelativeSource={RelativeSource TemplatedParent}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type core:ValidationMessage}">
                                    <TextBlock
                                        Foreground="#C42B1C"
                                        Text="{Binding DisplayText}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsRequired" Value="True">
                            <Setter TargetName="RequiredIndicator" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
