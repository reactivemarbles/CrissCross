<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:CrissCross.WPF.UI.Controls"
>
  <Style TargetType="{x:Type controls:Stepper}">
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="ItemTemplate">
      <Setter.Value>
        <DataTemplate>
          <Button
            MinWidth="96"
            Margin="0,0,8,8"
            HorizontalContentAlignment="Left"
            Command="{Binding JumpToStepCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:Stepper}}}"
            CommandParameter="{Binding}"
          >
            <StackPanel>
              <TextBlock FontWeight="SemiBold" Text="{Binding DisplayTitle}" />
              <TextBlock FontSize="11" Opacity="0.68" Text="{Binding Status}" />
            </StackPanel>
          </Button>
        </DataTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ItemsPanel">
      <Setter.Value>
        <ItemsPanelTemplate>
          <WrapPanel />
        </ItemsPanelTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:Stepper}">
          <StackPanel>
            <TextBlock
              Margin="0,0,0,8"
              Opacity="0.72"
              Text="{Binding State.ProgressText, RelativeSource={RelativeSource TemplatedParent}}"
            />
            <ItemsPresenter />
            <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
              <Button
                MinWidth="72"
                Margin="0,0,8,0"
                Command="{Binding PreviousStepCommand, RelativeSource={RelativeSource TemplatedParent}}"
                Content="Previous"
                IsEnabled="{Binding State.CanGoPrevious, RelativeSource={RelativeSource TemplatedParent}}"
              />
              <Button
                MinWidth="72"
                Margin="0,0,8,0"
                Command="{Binding NextStepCommand, RelativeSource={RelativeSource TemplatedParent}}"
                Content="Next"
                IsEnabled="{Binding State.CanGoNext, RelativeSource={RelativeSource TemplatedParent}}"
              />
              <Button
                MinWidth="72"
                Margin="0,0,8,0"
                Command="{Binding FinishCommand, RelativeSource={RelativeSource TemplatedParent}}"
                Content="Finish"
                IsEnabled="{Binding State.CanFinish, RelativeSource={RelativeSource TemplatedParent}}"
              />
              <Button
                MinWidth="72"
                Command="{Binding CancelCommand, RelativeSource={RelativeSource TemplatedParent}}"
                Content="Cancel"
              />
            </StackPanel>
          </StackPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
