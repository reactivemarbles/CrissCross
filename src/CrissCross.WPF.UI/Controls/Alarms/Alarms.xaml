<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CrissCross.WPF.UI.Controls">

    <DataTemplate DataType="{x:Type controls:AlarmItem}">
        <controls:AlarmBanner
            Margin="0,0,0,8"
            AcknowledgeText="{Binding AcknowledgeText}"
            IsAcknowledgeVisible="{Binding IsAcknowledgeVisible}"
            IsActive="{Binding IsActive}"
            IsClosable="{Binding IsClosable}"
            Message="{Binding Message}"
            Severity="{Binding Severity}" />
    </DataTemplate>

    <Style TargetType="{x:Type controls:Alarms}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Alarms}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                        <ItemsControl ItemsSource="{Binding AlarmItems, RelativeSource={RelativeSource TemplatedParent}}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
