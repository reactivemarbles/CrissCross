<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:CrissCross.WPF.UI.Controls"
>
  <Style TargetType="{x:Type controls:BezelToggleButton}">
    <Setter Property="Background" Value="{DynamicResource ControlSolidFillColorDefaultBrush}" />
    <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
    <Setter Property="Padding" Value="{DynamicResource ButtonPadding}" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="FocusBrush" Value="Transparent" />

    <Setter Property="BorderThickness" Value="5" />
    <Setter Property="MinorBorderThickness1" Value="5,5,5,5" />
    <Setter Property="CornerRadius1" Value="20" />
    <Setter Property="CornerRadius2" Value="20" />
    <Setter Property="MinorBackground1" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="PressedBrush" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BezelButtonBorderBrush}" />
    <Setter Property="MinorBorderBrush1" Value="{DynamicResource BezelButtonInnerBorderBrush}" />

    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:BezelToggleButton}">
          <Grid x:Name="PART_MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <controls:RaisedBorder
              x:Name="PART_RaisedBorder"
              Width="{TemplateBinding Width}"
              Height="{TemplateBinding Height}"
              MinWidth="{TemplateBinding MinWidth}"
              MinHeight="{TemplateBinding MinHeight}"
              HorizontalContentAlignment="Stretch"
              VerticalContentAlignment="Stretch"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius1="{TemplateBinding CornerRadius1}"
              CornerRadius2="{TemplateBinding CornerRadius2}"
              Foreground="{TemplateBinding Foreground}"
              GlareBrush="{TemplateBinding GlareBrush}"
              GlareOpacityMask="{TemplateBinding GlareOpacityMask}"
              MinorBorderBrush1="{TemplateBinding MinorBorderBrush1}"
              MinorBorderThickness1="{TemplateBinding MinorBorderThickness1}"
              Padding="{TemplateBinding Padding}"
            >
              <Grid Name="PART_DepressGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Border
                  x:Name="PART_On"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Background="{TemplateBinding PressedBrush}"
                  CornerRadius="{TemplateBinding CornerRadius1}"
                  Opacity="0.5"
                  Visibility="Visible"
                />

                <Border
                  x:Name="PART_Mid"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Background="{TemplateBinding MinorBackground1}"
                  CornerRadius="{TemplateBinding CornerRadius2}"
                  Opacity="0.5"
                  Visibility="Visible"
                />

                <ContentPresenter
                  x:Name="Content"
                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                  ContentPresenter.Content="{TemplateBinding ContentControl.Content}"
                  TextElement.Foreground="{TemplateBinding Foreground}"
                >
                  <ContentPresenter.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                      <Setter Property="Background" Value="Transparent" />
                      <Setter
                        Property="Foreground"
                        Value="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                      />
                      <Setter Property="TextWrapping" Value="Wrap" />
                      <Setter Property="TextAlignment" Value="Center" />
                    </Style>
                  </ContentPresenter.Resources>
                </ContentPresenter>
              </Grid>
            </controls:RaisedBorder>

            <Border
              x:Name="PART_UserHintBorder"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              BorderBrush="{TemplateBinding FocusBrush}"
              BorderThickness="{TemplateBinding FocusBorderThickness}"
              CornerRadius="{TemplateBinding CornerRadius1}"
            />
          </Grid>

          <ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsChecked" Value="True">
              <Setter TargetName="PART_On" Property="Visibility" Value="Visible" />
              <Setter TargetName="PART_Mid" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_MainGrid" Property="Margin" Value="2" />
            </Trigger>

            <Trigger Property="ToggleButton.IsChecked" Value="{x:Null}">
              <Setter TargetName="PART_On" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_Mid" Property="Visibility" Value="Visible" />
              <Setter TargetName="PART_MainGrid" Property="Margin" Value="2" />
            </Trigger>

            <Trigger Property="ToggleButton.IsChecked" Value="False">
              <Setter TargetName="PART_On" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_Mid" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_MainGrid" Property="Margin" Value="0" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter
                TargetName="PART_RaisedBorder"
                Property="Background"
                Value="{DynamicResource ButtonBackgroundDisabled}"
              />
              <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrushDisabled}" />
              <Setter Property="MinorBorderBrush1" Value="{DynamicResource ButtonBorderBrushDisabled}" />
              <Setter
                TargetName="Content"
                Property="TextElement.Foreground"
                Value="{DynamicResource ButtonForegroundDisabled}"
              />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
